// A generated protocol module (written by test/round-trip.test.ts's beforeAll into this same directory) imports its cbor-decoding helper as a same-directory sibling, "./runtime.js" -- matching where cddl.js's own CLI writes output alongside src/runtime.ts. This re-export gives it that sibling here too, without duplicating runtime.ts's content.
export * from "../../../src/runtime.ts";
